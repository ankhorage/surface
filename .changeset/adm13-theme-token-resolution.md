---
"@ankhorage/surface": minor
---

Resolve canonical authored theme token overrides into Surface runtime tokens while preserving defaults, validating invalid values explicitly, and keeping module-derived font families isolated from persisted theme source.
