---
'@ankhorage/surface': major
---

Move the remaining legacy component APIs into canonical feature ownership. `Field` and `Switch` now live under the form capability, `List` and `ListItem` share the new list capability, and `Textarea`, `Label`, and `HelperText` are removed in favor of `TextInput multiline` and field-owned presentation.
