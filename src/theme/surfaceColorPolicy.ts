/** Canonical measurable thresholds used while Surface resolves runtime color semantics. */
export const SURFACE_COLOR_POLICY = {
  disabledContrast: 3,
  surfaceSeparation: 1.05,
  textContrast: 4.5,
  uiContrast: 3,
} as const;
